server:
  port: 8081

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:/Users/Zileri/config
          searchPaths: config-data
#          username: ${GIT_USER}
#          password: ${GIT_PASSWORD}
#          default-label: "master"
 # security:
 #   user:
 #     name: root
 #     password: zileri  