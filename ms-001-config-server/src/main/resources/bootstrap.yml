server:
  port: 8081

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Zileri/CDO
          searchPaths: config-data
          username: Zileri
          password: Felizdia7
          default-label: "master"
  security:
    user:
      name: root
      password: zileri  