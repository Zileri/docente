spring:
  application:
    name: users-service
  cloud:
    config:
      uri: https://github.com/Zileri/CDO.git
      username: ${GIT_USER}
      password: ${GIT_PASSWORD}
 # boot:
 #   admin:
 #     client:
 #       url: http://localhost:8086